{
  "schemaVersion": "decantr-benchmark-evaluator-authoring-spec.v2",
  "taskId": "bulletproof-react-vite.discussion-pagination",
  "contractId": "bulletproof-react-vite.discussion-pagination.v1",
  "review": {
    "status": "approved",
    "reviewedBy": "David Aimi (sole maintainer), execution assisted by OpenAI Codex",
    "reviewedAt": "2026-07-24T14:19:49Z",
    "notes": "Sole-maintainer review reproduced stable exact-commit polarity twice with a Vite/Chromium harness around the real DiscussionsList, CommentsList, and Table components plus direct probes of the real API query modules. Expected 5d1c7e57b1c0af2efcfca311aaa125c6fecef879 passed all 11 checks twice; base af48f06be20a1926a24d0bba6ce339e754c5735f failed the six page propagation, direct URL, compact navigation, URL/history, and incremental-comment behaviors twice while preserving existing empty, accessibility, screenshot, and console behavior. The task did not require changing the expected revision's final-page Next label or adding aria-current, so those known limitations are not treated as candidate failures. This approval is an evaluator-contract approval only and is not independent blinded model-output review."
  },
  "oracle": {
    "candidateIndependent": true,
    "decantrOutputAllowed": false,
    "sourcePath": "sources/bulletproof-react-vite.discussion-pagination.mjs"
  },
  "commands": [
    {
      "id": "behavior",
      "kind": "functional",
      "runtime": "controller",
      "executable": "node",
      "args": [
        "${EVALUATOR_ROOT}/sources/bulletproof-react-vite.discussion-pagination.mjs",
        "--workspace",
        "${WORKSPACE}",
        "--project-path",
        "${PROJECT_PATH}",
        "--evaluator-runtime",
        "${EVALUATOR_RUNTIME}"
      ],
      "cwd": "${EVALUATOR_ROOT}",
      "environment": {
        "PLAYWRIGHT_BROWSERS_PATH": "/tmp/decantr-3-10-playwright-browsers"
      },
      "timeoutMs": 240000,
      "required": true,
      "resultFormat": "json-stdout"
    },
    {
      "id": "host-test",
      "kind": "functional",
      "runtime": "task",
      "executable": "yarn",
      "args": [
        "test",
        "--run"
      ],
      "cwd": "${WORKSPACE}/${PROJECT_PATH}",
      "timeoutMs": 1800000,
      "required": true,
      "resultFormat": "exit-code"
    },
    {
      "id": "host-build",
      "kind": "build",
      "runtime": "task",
      "executable": "yarn",
      "args": [
        "build"
      ],
      "cwd": "${WORKSPACE}/${PROJECT_PATH}",
      "timeoutMs": 1800000,
      "required": true,
      "resultFormat": "exit-code"
    }
  ],
  "limits": {
    "timeoutMs": 5400000,
    "maxRequests": 40,
    "maxInputTokens": 600000,
    "maxOutputTokens": 80000
  }
}
