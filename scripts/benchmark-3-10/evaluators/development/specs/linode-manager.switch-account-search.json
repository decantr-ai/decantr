{
  "schemaVersion": "decantr-benchmark-evaluator-authoring-spec.v2",
  "taskId": "linode-manager.switch-account-search",
  "contractId": "linode-manager.switch-account-search.v1",
  "review": {
    "status": "approved",
    "reviewedBy": "David Aimi (sole maintainer), execution assisted by OpenAI Codex",
    "reviewedAt": "2026-07-24T13:26:00Z",
    "notes": "Sole-maintainer approval after replacing the source-shape parser with an evaluator-owned Vitest scenario that renders the real SwitchAccountDrawer through repository test utilities and mocked IAM query boundaries. Two clean executions at frozen expected commit c30ee087ba9f91d6ba0e59bd8d4a37a70e2ca544 passed 2/2 behavioral checks; two clean executions at frozen base commit 235b26d39d939344c3f26f58dd53a815ac7bae7d failed 2/2. The oracle observes input identity, typed-value and focus retention across the real 400 ms debounce, suppresses the no-access state while loading, and requires it after an empty result settles. It does not inspect implementation source shape. This approval is maintainer qualification, not independent model-output review."
  },
  "oracle": {
    "candidateIndependent": true,
    "decantrOutputAllowed": false,
    "sourcePath": "sources/linode-manager.switch-account-search.mjs"
  },
  "commands": [
    {
      "id": "behavior",
      "kind": "functional",
      "runtime": "controller",
      "executable": "node",
      "args": [
        "${EVALUATOR_ROOT}/sources/linode-manager.switch-account-search.mjs",
        "--workspace",
        "${WORKSPACE}",
        "--project-path",
        "${PROJECT_PATH}"
      ],
      "cwd": "${EVALUATOR_ROOT}",
      "timeoutMs": 120000,
      "required": true,
      "resultFormat": "json-stdout"
    },
    {
      "id": "host-build",
      "kind": "build",
      "runtime": "task",
      "executable": "pnpm",
      "args": [
        "run",
        "build"
      ],
      "cwd": "${WORKSPACE}",
      "timeoutMs": 1800000,
      "required": true,
      "resultFormat": "exit-code"
    },
    {
      "id": "manager-tests",
      "kind": "functional",
      "runtime": "task",
      "executable": "pnpm",
      "args": [
        "--filter",
        "linode-manager",
        "test"
      ],
      "cwd": "${WORKSPACE}",
      "timeoutMs": 1800000,
      "required": true,
      "resultFormat": "exit-code"
    },
    {
      "id": "manager-typecheck",
      "kind": "functional",
      "runtime": "task",
      "executable": "pnpm",
      "args": [
        "--filter",
        "linode-manager",
        "typecheck"
      ],
      "cwd": "${WORKSPACE}",
      "timeoutMs": 1800000,
      "required": true,
      "resultFormat": "exit-code"
    }
  ],
  "limits": {
    "timeoutMs": 7200000,
    "maxRequests": 40,
    "maxInputTokens": 800000,
    "maxOutputTokens": 100000
  }
}
