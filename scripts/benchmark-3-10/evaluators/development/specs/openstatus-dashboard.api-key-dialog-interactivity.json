{
  "schemaVersion": "decantr-benchmark-evaluator-authoring-spec.v2",
  "taskId": "openstatus-dashboard.api-key-dialog-interactivity",
  "contractId": "openstatus-dashboard.api-key-dialog-interactivity.v1",
  "review": {
    "status": "draft",
    "reviewedBy": "OpenAI Codex independent evaluator reviewer",
    "reviewedAt": "2026-07-22T14:20:13Z",
    "notes": "Independent exact-commit review reproduced stable source-oracle polarity twice: base ed757d14304d4f693c8a0e5d6374dca945fb32b0 passed 2/5 checks and expected 255bc215222d9d7222c14f50516d1f017c7dc934 passed 5/5. BLOCKED: the evaluator requires the expected Popover and cleanup-handler source forms and executes extracted handlers against a synthetic style object. It does not render the Radix dialog/popover stack or repeat picker close, Cancel, Escape, successful creation, success-dialog close, hit-testing, and focus assertions. Equivalent fixes can fail while a source-shaped but ineffective fix can pass. Keep draft until a deterministic component/browser harness observes page interactivity at both commits."
  },
  "oracle": {
    "candidateIndependent": true,
    "decantrOutputAllowed": false,
    "sourcePath": "sources/openstatus-dashboard.api-key-dialog-interactivity.mjs"
  },
  "commands": [
    {
      "id": "behavior",
      "kind": "functional",
      "runtime": "controller",
      "executable": "node",
      "args": [
        "${EVALUATOR_ROOT}/sources/openstatus-dashboard.api-key-dialog-interactivity.mjs",
        "--workspace",
        "${WORKSPACE}",
        "--project-path",
        "${PROJECT_PATH}"
      ],
      "cwd": "${EVALUATOR_ROOT}",
      "timeoutMs": 120000,
      "required": true,
      "resultFormat": "json-stdout"
    },
    {
      "id": "host-build",
      "kind": "build",
      "runtime": "task",
      "executable": "pnpm",
      "args": [
        "--filter",
        "@openstatus/dashboard",
        "build"
      ],
      "cwd": "${WORKSPACE}",
      "timeoutMs": 1800000,
      "required": true,
      "resultFormat": "exit-code"
    },
    {
      "id": "dashboard-typecheck",
      "kind": "functional",
      "runtime": "task",
      "executable": "pnpm",
      "args": [
        "--filter",
        "@openstatus/dashboard",
        "tsc"
      ],
      "cwd": "${WORKSPACE}",
      "timeoutMs": 1800000,
      "required": true,
      "resultFormat": "exit-code"
    }
  ],
  "limits": {
    "timeoutMs": 5400000,
    "maxRequests": 40,
    "maxInputTokens": 800000,
    "maxOutputTokens": 100000
  }
}
