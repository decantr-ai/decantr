# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml
#
# This directory entry runs the published Decantr 3.11.1 stdio server with no configuration.
# The existing decantr_verify tool adds changed-UI assurance through action: changes. Task
# preparation remains authority-aware and fails closed on ambiguous or unsupported targets.
# Smithery auto-indexes this file when the repo is claimed.

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction:
    |-
    (config) => ({
      command: 'npx',
      args: ['-y', '@decantr/mcp-server@3.11.1'],
      env: {}
    })
