# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml
#
# This directory entry runs the published Decantr 3.10.0 stdio server with no configuration.
# Task preparation uses authority-aware route and non-route UI-surface discovery and fails
# closed when the selected target is ambiguous, unsupported, or not taskable.
# Smithery auto-indexes this file when the repo is claimed.

startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction:
    |-
    (config) => ({
      command: 'npx',
      args: ['-y', '@decantr/mcp-server@3.10.0'],
      env: {}
    })
