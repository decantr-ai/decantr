

Operating as Principal Decantr Engineer with full architecture awareness. I understand the V3.1 composition pipeline, three-tier context model, 14 MCP tools, composition topology, and registry flow. Ready to work systemically.

Let me explore the `@decantr/css` package to understand the atom system.
Now I understand the system. Container atoms are static entries in the `DIRECT` map in `atoms.ts`. Let me read the file and add them.
Now I see the existing `container` atom on line 280 maps to `container-type:inline-size` (for CSS container queries). The new container width atoms need different names. The spec says `_container`, `_containersm`, etc. — but `container` is already taken. Let me note that `container` is for container queries, and the new atoms will be the max-width containment variants. Since the spec explicitly asks for `_container` which would conflict, I'll use exactly the names specified but need to handle the collision.

Looking more carefully — the existing `container` atom is `container-type:inline-size`. The new `_container` would be `max-width:1200px;margin-inline:auto;width:100%;padding-inline:1rem`. These conflict. I'll rename the existing one to `cqinline` (container query inline) and use `container` for the new max-width containment, since that's the more common developer expectation.

Actually, let me keep it simple — the spec says add these atoms. The existing `container` maps to CSS container queries which is a niche use. The max-width container pattern is far more common and what LLMs need. I'll replace `container` with the new meaning and add `cqinline` for the old one.

Wait — let me check if `container` is used anywhere first.
No tests reference `container`. The `_container` grep hits are unrelated (variable names, not atom usage). Safe to replace the existing `container` atom.

I'll rename the existing container-query atom to `cqinline` and add all the new container width atoms.
Now let me read the test file to add tests.
Now let me add the container atom tests and then build + test.
Now build and test.
All 44 tests pass (31 atom tests including the 7 new container tests, plus 13 css tests).

Here's what I did:

- **Renamed** the existing `container` atom (CSS container queries) to `cqinline` to avoid collision
- **Added 6 container width atoms** to the `DIRECT` map in `atoms.ts`:
  - `_container` — 1200px max-width
  - `_containersm` — 640px
  - `_containermd` — 768px
  - `_containerlg` — 1024px
  - `_containerxl` — 1400px
  - `_containerfull` — 100%
- All share: `margin-inline:auto; width:100%; padding-inline:1rem`
- **Added 7 test cases** in `atoms.test.ts` covering all new atoms plus the renamed `cqinline`

**Downstream impact**: The old `_container` atom (container queries) is now `_cqinline`. If any code used `_container` for container queries, it needs updating — but the grep showed no usage in the monorepo.
