

Operating as Principal Decantr Engineer with full architecture awareness. I understand the V3.1 composition pipeline, three-tier context model, 14 MCP tools, composition topology, and registry flow. Ready to work systemically.

Let me start by reading the existing CLI structure to understand the patterns before implementing.
Good, I have a clear picture. Let me read the relevant parts of `index.ts` — the command switch and the init flow.
Now let me check the color/formatting utilities and types used across the CLI.
Now let me also check the `InitArgs` interface and how `cmdInit` finishes (the output section).
Now I have the full picture. Let me implement the `new` command. I'll create the command file and modify `index.ts`.


Now let me add the `new` command to `index.ts`.
Now add the `new` case to the switch statement. I'll add it right before the `init` case since it's a related project creation command:
Now update the help text:
Add an example:
Now build and test:
Build succeeds. Now run tests:
All 719 tests pass, build succeeds.
Done. Here's what was implemented:

**New file:** `packages/cli/src/commands/new-project.ts`
- `cmdNewProject(projectName, options)` — creates a complete project from scratch
- Creates directory, `package.json` (Vite + React 19 + react-router-dom + @decantr/css), `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.tsx`, `src/App.tsx`
- Detects package manager from parent context (pnpm/yarn/bun/npm)
- Runs `<pm> install` then `decantr init --yes --existing` with all passthrough flags (blueprint, archetype, theme, mode, shape, offline, registry)
- Validates project name and checks directory doesn't already exist

**Modified:** `packages/cli/src/index.ts`
- Added `case 'new'` to the command switch with flag parsing
- Updated `cmdHelp()` with usage, command description, and example

**Usage:**
```bash
decantr new my-app                                  # Defaults
decantr new my-app --blueprint=carbon-ai-portal     # With blueprint
decantr new my-app --archetype=saas-dashboard       # With archetype
decantr new my-app --theme=obsidian --mode=dark     # Custom theme
```
