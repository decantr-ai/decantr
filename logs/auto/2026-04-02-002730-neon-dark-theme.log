

Operating as Principal Decantr Engineer with full architecture awareness. I understand the V3.1 composition pipeline, three-tier context model, 14 MCP tools, composition topology, and registry flow. Ready to work systemically.

Let me create both files. First, let me verify the staging directory exists.
Both target directories exist. Let me also check an existing theme and recipe for schema reference.
Empty directories. Let me check the existing carbon theme in the cache for schema alignment.
No cached themes to reference. The schema provided in the prompt is clear. Let me write both files.
Both files created:

- **Theme**: `tools/auto-tasks/staging/themes/neon-dark.json` — void dark (#0A0A0F) background, electric cyan (#00E5FF) primary, soft purple (#B388FF) secondary, neon green (#76FF03) accent
- **Recipe**: `tools/auto-tasks/staging/recipes/neon-cyber.json` — 7 decorators (neon-card, neon-input, neon-glass, neon-code, neon-glow, neon-divider, neon-badge) with comfortable density and soft card wrapping

The decorator descriptions are keyword-rich for the `generateDecoratorRule` heuristic engine — terms like "backdrop blur(16px)", "monospace", "box-shadow glow", and "gradient from transparent" will map cleanly to CSS output during scaffold.
