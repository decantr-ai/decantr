{{> _base}}

## Task: Add preset to existing pattern

**Gap:** {{gapName}} -> preset on existing pattern
**Location:** {{location}}

**Implementation:**
```javascript
{{gapCode}}
```

**Requirements:**
1. Find the matching pattern in src/registry/patterns/
2. Add new preset to the pattern's `presets` object
3. Include: description, components array, blend config, code example
4. Follow existing preset naming conventions
