{{> _base}}

## Task: Improve existing pattern

**Pattern:** {{gapName}}
**Opportunity:** Gap code shows variant not covered by existing presets

**Gap code:**
```javascript
{{gapCode}}
```

**Requirements:**
1. Read the existing pattern file
2. Identify what the gap code adds that's missing
3. Either add a new preset or enhance existing ones
4. Maintain backwards compatibility
