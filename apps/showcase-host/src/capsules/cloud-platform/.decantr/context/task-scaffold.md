# Task Context: Scaffolding

**Enforcement Tier: Creative** — Guard rules are advisory during initial scaffolding.

## What to Generate

- **home** (full-bleed)
  - Patterns: hero (brand) as brand-hero, card-grid (icon) as feature-grid, logo-strip (marquee) as tech-logos, card-grid (icon) as capability-grid, stats-section (hero) as platform-stats, cta-section (brand) as enterprise-cta, footer (minimal) as footer
- **cloud-infrastructure-apps** (full-bleed)
  - Patterns: filter-bar, [card-grid (resource) as app-grid | activity-feed] @lg
- **cloud-infrastructure-app-detail** (full-bleed)
  - Patterns: detail-header, kpi-grid, [log-stream (live) as log-stream | data-table] @lg, request-trace
- **cloud-infrastructure-team** (full-bleed)
  - Patterns: filter-bar, card-grid (user) as team-grid
- **cloud-infrastructure-activity** (full-bleed)
  - Patterns: filter-bar, activity-feed
- **cloud-infrastructure-services** (full-bleed)
  - Patterns: service-catalog (grid) as service-grid, topology
- **cloud-infrastructure-tokens** (full-bleed)
  - Patterns: filter-bar (toolbar) as token-toolbar, data-table
- **cloud-infrastructure-usage** (full-bleed)
  - Patterns: stats-overview (gauge-grid) as resource-gauges, chart-grid
- **cloud-infrastructure-status** (full-bleed)
  - Patterns: status-board
- **cloud-infrastructure-compliance** (full-bleed)
  - Patterns: checklist-card (onboarding) as compliance-checklist, data-table
- **billing-billing** (full-bleed)
  - Patterns: kpi-grid, pricing, payment-history
- **auth-flow-login** (full-bleed)
  - Patterns: auth-form
- **auth-flow-register** (full-bleed)
  - Patterns: auth-form (register)
- **auth-flow-forgot-password** (full-bleed)
  - Patterns: auth-form (reset)
- **settings-settings** (full-bleed)
  - Patterns: form-sections (settings)

Post-scaffold enforcement mode: **strict**.

---

*Task context generated by Decantr CLI*
